apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: page-scraper-pvc
  namespace: default
  labels:
    app.kubernetes.io/name: page-scraper-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
